import test from 'node:test';import assert from 'node:assert/strict';import {getDataset} from '../server/repository.mjs';
test('Supabase repository sends publishable key and maps rows',async()=>{let seen;const fetcher=async(url,options)=>{seen={url:String(url),headers:options.headers};return {ok:true,json:async()=>String(url).includes('offset=0')?[{id:'1',name:'테스트',lat:37.5,lng:127,address:'서울',hours:'24시간',type:'공공',retrieved_at:'2026-09-14'}]:[]};};const data=await getDataset({DATA_SOURCE:'supabase',SUPABASE_URL:'https://unit-test.supabase.co',SUPABASE_PUBLISHABLE_KEY:'publishable'},fetcher);assert.equal(data.dataSource,'supabase');assert.equal(data.rows.length,1);assert.equal(seen.headers.apikey,'publishable');assert.equal(seen.headers.Authorization,'Bearer publishable');assert(seen.url.includes('/rest/v1/toilets'));});
