{
  "name": "skibidi-toilet",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "node --watch server/local.mjs",
    "start": "node server/local.mjs",
    "build": "node scripts/build-vercel.mjs",
    "test": "node --test tests/*.test.mjs"
  }
}
